# 验收记录 · I5 会议卡 + 慢路径 + 资料库

日期：2026-09-17 · 执行：Claude · 提交 `4602337`

## 1. 交付物

| 项 | 状态 | 位置 |
|---|---|---|
| 会议录音 `MeetingRecorder`：一按即录 → 端侧流式转写 → 落库（sessions / segments）；暂停 / 继续、书签、音频时钟、代际守卫（stop 重入返回空） | ✅ | `meeting/MeetingRecorder.kt` |
| 慢路径 `SlowPath`：有 Key → `qwen-flash` JSON 模板成稿（会议纪要 / 对话卡片）；零 Key → 规则版「时间轴要点」；脱敏 `Redactor`（成稿前替换手机 / 邮箱 / 证件号，成稿后还原）；事实守恒 `FactCheck`（数字 / 日期 / 金额必须出现在原文）；按风格缓存 `polish_revisions` 可回滚 | ✅ 单测 | `polish/SlowPath.kt`、`polish/Llm.kt` |
| 产物与分享：Markdown / SRT / VTT / `.voxnote`（JSON 打包）/ 卡片 PNG；系统分享面板（Android FileProvider、iOS UIActivityViewController） | ✅ | `polish/Exports.kt`、`androidMain/.../AndroidSharer.kt`、`iosMain/.../IosSharer.kt` |
| 页面：`Meeting`（计时 + 波形 + 流式草稿 + dock）、`MeetingResult`（模板成稿 ⇄ 时间轴要点、风格切换、分享）、`LiveEnd`（对话卡片：双语对照 + 要点 + 新词候选确认）、`Library` 资料库 Tab（对话 / 纪要 / 字幕分段、按日分组、删除） | ✅ 截图 `截图/I5-vivo-*.png` | `ui/meeting/`、`ui/note/`、`ui/library/` |
| 数据层：`session_note`（成稿 / 字幕译文）、`bookmark`、`glossary_candidate`、全文搜索（LIKE + ESCAPE）、事务删除 | ✅ | `sqldelight/.../Notes.sq`、`core/db/SessionRepository.kt` |
| 审查修复：`INSERT OR REPLACE` 级联删段改为 `updateAudioPath`；LIKE 特殊字符转义；导出改 suspend + 安全文件名；MeetingRecorder 原子状态更新与取消顺序；NoteViewModel 切换会话重置状态；摘要模式对纯文本做事实守恒 | ✅ | 见各文件 |

## 2. 已在真机确认（vivo V2436A）

- 会议：开始 → 说话流式草稿 → 停止 → 自动进纪要页；零 Key 与有 Key 两条链路都能出稿（有 Key 时模板成稿 ≈ 5–8 s）。
- 分享面板能弹出，`.md` 文件可发到微信 / QQ。

## 3. 需要你来测的

1. **真实会议 20 分钟**：纪要结构（决定 / 待办 / 议题）是否可用；事实守恒是否误拦（数字被改写时纪要页会提示「有出入」）。
2. 纪要措辞：`Templates.minutesSystem` 里的模板语气（商务 / 中性）是否合适，改词直接改这里。
3. 对话结束页的「新词」候选：确认后应出现在设置 → 术语表（I7 已接）。
4. iOS：分享面板到 AirDrop / 备忘录一次。

## 4. 未做 / 后移

- 卡片 PNG 只画文字版（无品牌视觉），v1.1 做设计稿。
- 会议中的云端 ASR 首包降级链（I3 决策：先只做端侧转写）。
