# 13 · 执行计划（精简 MVP · 你 + Claude · 双端同步）

> **这篇讲什么**：把 10-路线图（6 人 18 周）压成"1 位真机操作者 + Claude 写代码"的可执行版本：范围砍到"实时线优先精简版"，按里程碑而不是按周推进，每个里程碑有交付物、验收标准、以及你要亲手做的真机事项。
> **决策来源**：2026-09-17 拍板——直接在本仓库写代码；人力 = 你 + Claude；Android / iOS 双端同步交付；MVP 取"实时线优先精简版"。
> **版本来源**：repo1.maven.org / dl.google.com 的 maven-metadata 与 GitHub Releases（2026-09-17 核对），组合以 JetBrains 官方 KMP-App-Template 为准。

## 先看这个

1. **范围**：旗舰屏外 **M0 仅听 / M1 耳听·面屏 / M3 双屏对话 / M4 速译卡片**（中 ↔ 英自动判向 + 粤 / 川单工 M0）+ **会议卡**（录音线最小闭环）+ 屏内 **S4 文件字幕 / S8 外放旁听**（双端）+ **Android S1 系统字幕**（关卡 B 之后）。
2. **延后到 v1.1**：口述成稿卡、方言家庭卡、`.scene` 导入导出、P2P（M8+）、手表、Widget（只留最小入口）、Koog、Android S3 OCR、Live Activity。
3. **两道关卡保留**：关卡 A（= 路线图第 4 周）基线机模型基准与离线档 M0；关卡 B（= 第 10 周）旗舰机混合档延迟、拔耳机不外放、判向准确率、待机电量。**关卡不过就砍功能，不砍质量。**
4. **分工**：Claude 写全部 Kotlin / Swift / Gradle / CI / 基准脚本并保证双端编译、单元测试通过；你负责真机运行与实测记录、Apple 开发者账号与签名、TestFlight / 内测 apk 分发、模型下载与许可复核、以及关卡拍板。
5. **节奏**：以里程碑 I0–I8 推进（2026-09-17 在 I2 与 I3 之间加入 **I2.5 壳迁移**：把 material3 占位壳换成 `core/designsystem`，页面以 14 篇原型为验收依据），每个里程碑结束时双端都能构建运行、验收项写入 `docs/验收记录/`。里程碑内部不再分周。

## 决策记录（2026-09-17）

| 决策 | 内容 | 影响 |
|---|---|---|
| 耳机不作为功能 | 耳机只是一个输出设备，App 不关心，外放也可以 | 删除"耳机模式铁律"与所有耳机相关验收；iOS 会话加 `defaultToSpeaker`；M5 分耳对话移出范围；自检页只展示路由 |
| iOS 最低版本 18 | 部署目标 18.0 | Apple Translation（iOS 18+）可作离线翻译档；能力分层只剩 18 / 26 |
| 只在旗舰机上测 | 不准备基线机 | 关卡 A 的内存分级按文档预设，不做基线机实测 |
| Android 只在 vivo 上测 | V2054A（Android 11） | OEM 矩阵延后到灰度期 |
| 极限性能测试延后 | 待机 8 h 电量、30 分钟连续发热/功耗、三姿态×三噪声矩阵移到灰度期 | 关卡 B 只保留句尾→首音延迟与判向准确率 |
| 方言优先级 | 先四川话（川渝包），粤语其次 | I2 清单：Silero VAD / 中英流式 zipformer / SenseVoice / 川渝 Paraformer（`sherpa-onnx-paraformer-zh-int8-2025-10-07`）/ 声纹 CAM++，全部按需下载 |
| 云端 provider | 先只接阿里云百炼 | I3 的 Anthropic / OpenAI 兼容端点延后 |
| TEAM_ID | 只用于真机签名，必须是 10 位 Apple Team ID；bundle id 固定 `dev.scenenote.app`，不再拼接 TEAM_ID | 模板工程的 `${BUNDLE_ID}${TEAM_ID}` 已改掉 |
| 端侧 TTS 选型（I3 实测后） | 系统 TTS 默认（iOS AVSpeechSynthesizer.write 取 PCM；Android TextToSpeech.synthesizeToFile 取 WAV，vivo V2054A 实测有 `com.vivo.aiservice` 引擎、首音 0.2–0.4 s）；端侧兜底改为 **matcha-icefall-zh-en + vocos-16khz**（16 kHz 与 AudioSink 一致、Apache-2.0、含 espeak-ng-data 子集共 135 MB），不用 MeloTTS / Kokoro（树莓派 4 单线程 RTF 6.7 / 7.6，骁龙 480 实测 Matcha RTF≈1.1 已是可用下限） | `ModelCatalog.ttsMatchaZhEn`；TTS 输出路径唯一（PCM → PlaybackQueue → AudioSink）；`TtsRouter` 偏好 auto / system / local / off |
| 云端翻译模型 | 默认 `qwen-mt-flash`（官方通用首选、支持增量流式；turbo 已停更；lite 最快但无粤语；plus 质量最高不支持增量流式） | `translation_options` 放 body 顶层且必传；只允许一条 user 消息、无 system；北京 RPM 60（≈1 句/秒）；Key 与地域绑定；旧域名 dashscope.aliyuncs.com 2026-09-30 后不再加新特性，Base URL 可在 Key 钱包覆盖 |
| 端侧 NMT（core/nmt）后移为 I3.5 | I3 先交付"云端 + 无 Key 诚实降级"；opus-mt ONNX（Xenova 导出 int8 每方向 ≈ 115 MB，非文档写的 80 MB）需自写 SentencePiece（≈ 500 行）+ ORT 绑定：Android 复用 sherpa AAR 的 libonnxruntime.so（VERS_1.28.2 符号）+ Maven onnxruntime-android 1.28.0 的 classes.jar + csukuangfj 1.28.2 zip 的 libonnxruntime4j_jni.so；iOS 对 onnxruntime_c_api.h 另起 cinterop | 一个 session 可做完分词 + 贪心解码 + Android 跑通；iOS 与基准再 1–2 个 session。ML Kit Translation 的模型从 dl.google.com 下载（国内需 VPN），系统翻译插件维持 v1.1 |
| 内存分级落地 | `MemoryTier`（< 4 GB LOW / 4–6 MID / ≥ 6 HIGH，读 /proc/meminfo 或 physicalMemory）；LOW 实时档只驻留 zipformer + VAD（vivo 实测 zipformer + VAD + SenseVoice 已 700 MB PSS，四模型 899 MB） | `LoadPlan.forLang(lang, tier)`；四川话仍装川渝 Paraformer |
| I2.5 / I3 落地状态（2026-09-17） | I2.5 壳与全部页面迁移完成、material3 已删；I3 管线双端跑通（云翻译待用户 Key 实测）；验收记录 `docs/验收记录/I3.md` 含"需要你来测的"清单 | CI 截图矩阵、端侧 NMT（I3.5）、Anthropic/OpenAI 端点未做 |
| I4 范围与默认值（2026-09-17） | 按已有决策裁剪：不做拔耳机外放脚本 / 8 h 电量 / 三姿态×三噪声；待机档预热移 I4.5（需后台音频真机验证）。默认：**姿态自动进 M1 = 开**、**判向自动 = 开**（脚本 + 声纹 + 两句防抖），开场白默认关；M0 ⇄ M1 ⇄ M3 在同一会话、同一 VM 内切换不重启音频 | `docs/验收记录/I4.md`、`关卡B.md` |
| I4 落地状态（2026-09-17） | 判向 / ducking / 热阶梯 / M0⇄M1⇄M3 同会话切换 / 姿态 / 媒体键 / 磁贴 / AppIntents / 关卡 B 统计全部落地并双端编译；68 单测；vivo 传感器与媒体会话已确认；耳机键手感、姿态阈值、热状态、iOS 快捷指令待真机 | `docs/验收记录/I4.md`、`关卡B.md` |
| 验收脚本"喂 WAV" | 会话页深链 `scenenote://scene/<id>?autostart=1&feed=<bench 内文件>&other=zh-CN&my=zh-CN`：用 WAV 回放替代麦克风，同语对 = 恒等翻译，可在无 Key、无人说话时验证 ASR → 翻译 → TTS → 播放全链路 | `FileAudioSource`；adb 里 `&` 要写成 `\&` |

## 版本锁定（I0 起生效）

| 项 | 版本 | 说明 |
|---|---|---|
| Kotlin | 2.4.10 | 2.4.20 已发布，待官方模板跟进后再升 |
| Compose Multiplatform | 1.12.0 | 依赖全部显式坐标（`compose.*` 访问器在 1.12 已弃用）；material3 锁 1.9.0（插件内置映射） |
| AGP / Gradle | 9.1.1 / 9.6.1 | shared 用 `com.android.kotlin.multiplatform.library` 插件（AGP 9 推荐方式） |
| JDK | 21（JBR） | 本机已装 |
| compileSdk / targetSdk / minSdk | 37 / 37 / 26 | Compose 1.12.0 / okhttp 5.5 要求 compileSdk ≥ 37（I0 实测）；minSdk 26 沿用方案，系统 SQLite 3.18 → SQL 不用 UPSERT |
| iOS Deployment Target | 18.0 | 决策 2026-09-17；能力按 18 / 26 分层 |
| kotlinx-coroutines / serialization / datetime / io | 1.11.0 / 1.11.0 / 0.8.0 / 0.9.1 | |
| Ktor | 3.6.0 | client-core + okhttp / darwin + content-negotiation + logging |
| SQLDelight | 2.3.2 | android-driver / native-driver |
| Koin | 4.2.2 | koin-core + koin-compose + koin-compose-viewmodel |
| Navigation Compose（KMP） | 2.9.2 | 替代方案里的 Decompose，少一层依赖 |
| Lifecycle ViewModel（KMP） | 2.11.0 | |
| multiplatform-settings | 1.3.0 | 普通偏好；Key 用自研 SecureStore（Keychain / Keystore） |
| Okio | 3.18.2 | Vault 文件读写 |
| sherpa-onnx | 1.13.8（2026-09-10） | Android：GitHub Release `sherpa-onnx-1.13.8.aar`；iOS：xcframework + cinterop `c-api.h`；不在 Maven Central |
| onnxruntime | 随 sherpa-onnx（1.13.8 内置版本） | `core/nmt` 与 sherpa-onnx **共用同一份 ORT**：Android 复用 sherpa AAR 内置的 libonnxruntime（不再单独引 onnxruntime-android），iOS 复用 sherpa xcframework 的 onnxruntime 静态库；若必须独立链接则锁定同一版本（共用冲突待验证，附录 B 51） |
| opus-mt zh↔en int8 + SentencePiece | Helsinki-NLP 模型（CC-BY-4.0 待核实） | `core/nmt` 端侧 NMT 语言对包，每方向 ≈ 80 MB，按需下载（与方言包同通道）；两端零 Key 翻译默认离线档 |
| Room / DataStore | 不用 | 统一 SQLDelight + multiplatform-settings |
| media3 | 1.11.1 | Android 端 S4 抽音频 / 播放器 |
| ML Kit Translate / Apple Translation | 不引入（v1.1 可选插件） | 系统翻译降为 `FastTranslator` 下的可选插件，设置里手动启用；MVP 不实现、不依赖 |
| compose-cupertino / haze / Koog | 不引入 | cupertino 坐标不在 Maven Central；haze 2.0 仍 rc；Koog 延后 |

## 里程碑

| 里程碑 | 交付物 | 验收标准 | 你要做的 |
|---|---|---|---|
| **I0 工程基线** | KMP 骨架（shared / androidApp / iosApp）、版本目录、模块分层与包结构、Koin、SQLDelight 库表、multiplatform-settings + SecureStore、Egress 出口门面 + 出站账本、音频采集 / 路由 / 播放的 expect-actual 骨架、Compose 壳（首页场景卡 + 实时页 + 设置页占位）、GitHub Actions 双端构建、`.gitignore` | `./gradlew :androidApp:assembleDebug` 与 `xcodebuild -sdk iphonesimulator` 通过；两端能启动到首页 | 在 Android 真机 + iPhone 上各跑一次；提交 baseline commit |
| **I1 采集与播放** | Android AudioRecord / iOS AVAudioEngine 16 kHz mono PCM16 流；音频会话（iOS playAndRecord + defaultToSpeaker + A2DP；Android MODE_NORMAL + 焦点）；双端 AAC .m4a 落盘（Android MediaCodec+MediaMuxer；iOS ExtAudioFile）；AudioSink 背压播放；录音自检页；LatencyProbe 打点骨架；`scenenote://` 深链 + `--deeplink=` 启动参数（验收脚本化） | 说话时电平表有反应；停止后 m4a 可回放；测试音能从当前输出设备听到；深链自动录 N 秒可脚本验证 | iPhone + vivo 各跑一次「设置 → 录音自检」 |
| **I2 端侧识别 + 关卡 A** | sherpa-onnx 双端集成（Android AAR、iOS xcframework cinterop）；流式 zipformer 中英 + SenseVoice 定稿 + 声纹 CAM++（sherpa-onnx 为唯一端侧引擎，系统识别不接）；**`core/nmt` 第一版：ORT seq2seq 解码 + SentencePiece + opus-mt 中 ↔ 英语言对包**；模型管理（随包基础包 + NMT 语言对包 / 方言包直链下载 + 手动导入 + 校验）；两阶段识别（流式 → 本地定稿）；S0 规则层（口癖 / 断句 / 标点 / 数字归一）；**基准脚本可跑**（RTF / 首包 / 内存 / 耗电） | **关卡 A**：基线机（iPhone 12 / 骁龙 7 系）各模型 RTF、内存、耗电有数据；**NMT 单句延迟 / 内存 / 与 ASR + TTS 同跑发热有数据**；离线档 M0 中 ↔ 英句尾 → 首音 ≤ 2.5 s（翻译走端侧 NMT） | 提供基线机与旗舰机各一台；跑基准脚本并把数据贴进 `docs/验收记录/关卡A.md`；决定内存分级 |
| **I2.5 壳迁移（设计系统）** | 把 I0 的 material3 占位壳换成 `core/designsystem`（07 篇 §7.14、14 篇）：四 Tab 壳（`GlassScaffold` + `SceneTabBar`：场景 / 实时 / 资料库 / 设置）替换 NavHost 单栈；`Home`（五张场景卡 + 更多折叠）、`Live`（屏外 / 屏内分段、语言对、档位、升级阶梯）、`Settings`（inset grouped 六组）三页按原型重做；删除 `compose.material3` 依赖与 `SceneNoteTheme` 内层 `MaterialTheme`；Models / SelfTest 两页同步换组件（不改功能） | 双端启动到四 Tab 首页；原型 `docs/原型/` 的 Home / Live / Settings 三屏与真机截图逐项对得上；CI 截图矩阵（深色 × 最大字号 × 降低透明度 × 提高对比度）通过；`grep material3` 为空 | 在 iPhone + vivo 上各过一遍四 Tab；拍板 Android 字体（待决策 30） |
| **I3 实时管线 + M0 / M4** | 快路径：Segmenter → Direction（声纹 + 输出脚本、两句防抖）→ Translate（端侧 NMT `core/nmt` opus-mt 中 ↔ 英为默认离线档；BYOK 混合档：百炼 qwen-mt + Anthropic Haiku + OpenAI 兼容，1.5 s 超时降级到端侧 NMT；术语 S0 占位符回填）→ TTS（系统 TTS 取 PCM）→ PlaybackQueue → AudioSink；Stage Health 状态机；Key 钱包（粘贴 / 测试 / 消费闸门，原型 `KeyWallet` sheet）；M0 仅听（震动语义、耳语档，原型 `LiveM0`：28 pt 当前句 + 健康胶囊 + 玻璃 dock）；M4 速译卡片（原型 `LiveM4`：按住 → 松手 → 大字卡）；出门预热引导第 2 页「零 Key 能 / 不能」（原型 `Onboarding`）；粤 / 川单工 M0 | M0 中 ↔ 英混合档在旗舰机上跑通 30 分钟不断流；M4 一句话 ≤ 3 步 | 填自己的百炼 / Anthropic Key 实测费用换算；粤语包实测 |
| **I4 M1 / M3 + 关卡 B** | M1 耳听·面屏（姿态检测、礼貌卡、呼吸灯 / 对勾、退出语；原型 `LiveM1`：对方半屏旋转 180°、≥ 28 pt）；M3 双屏（摘耳机自动降级、180° 翻转；原型 `LiveM3`：常驻深色、中央玻璃胶囊、黄标外放默认关）；软半双工 ducking；待机档预热 + 出门预热引导第 1 / 3 页「三姿态」「入口绑定」（原型 `Onboarding`）；触发入口（耳机媒体键仅会话期、iOS AppIntent / Action Button、Android 磁贴）；热 / 电四级阶梯 | **关卡 B**：旗舰机混合档 P50 ≤ 1.5 s / P95 ≤ 2.2 s（本机口径 1300 / 2000 ms）；拔耳机 100% 不外放；三姿态 × 三噪声判向准确率；待机 8 小时电量 | 按 `docs/验收记录/关卡B.md` 的脚本逐项实测；拍板：判向默认自动还是固定、M1 姿态默认开还是手动 |
| **I5 会议卡 + 慢路径** | 会议场景卡（一按即录 → 端侧转写 → S0）；慢路径 S1–S4（结构化纪要 / 事实守恒 / 脱敏 / 缓存 / 降级矩阵）+ 零 Key 规则版「时间轴要点」；polish_revisions 回滚；产物：Markdown / .voxnote / 卡片 png；系统分享 + AirDrop / Quick Share；对话卡片（来自 M0 / M1 会话）。页面：`Meeting` 录音中（计时 + 波形 + 流式草稿 + dock）、`MeetingResult` 纪要（有 Key 模板成稿 ⇄ 零 Key 时间轴要点）、`LiveEnd` 对话卡片（双语对照 + 要点 AI 标记 + 新词候选占位）、**`Library` 资料库 Tab 列表页**（对话 / 纪要 / 字幕 / 留言卡分段，按日分组；搜索框留到 I7） | 一按之后 10 分钟内交出能分享的稿子（有 Key = 模板成稿，零 Key = 规则稿） | 用真实会议录音验收；决定纪要模板措辞 |
| **I6 屏内 S4 / S8 + Android S1** | S4：分享面板 / 相册 / 文件 / 直链 MP4 入口（原型 `ScreenIntake`：剪贴板直链提示、已处理列表、平台诚实清单）、抽音频（AVAssetReader / media3）、边转边看 + 自动暂停、精修轨、SRT / VTT 导出、生词抽屉（原型 `ScreenS4`：常驻深色播放器、双进度条、Waiting 遮罩、快路径轨 ⇄ 精修轨、玻璃 dock）；S8 = M0 媒体档；Android S1：MediaProjection + AudioPlaybackCapture + 2 s 可抓性探测 + PiP 字幕条 + 磁贴 + 披露页 + OEM 引导 | 10 分钟 1080p 首条字幕 ≤ 5 s、整片 ≤ 1/4 时长；S1 冷启动 3 步 ≤ 8 s、首条 ≤ 3 s | 在 YouTube / B 站 / 网课 App 实测可抓性并填兼容表；小米 / 华为 / OPPO 至少一台 |
| **I7 数据层回流 + 入口** | 术语表 + 纠错映射 + 实时 terms 注入（`LiveEnd` 新词候选「确认 → 进术语表」）；去向账本 UI + 四档隐私（原型 `Ledger`：今天 / 7 天、三数字块、逐条去向；`Settings` 隐私组）；本地通知（录后 10 分钟 / 3 天）；最小入口（iOS 静态 Widget + 控制中心；Android Glance 小组件；`Settings` 入口组）；全文检索（`Library` 底部悬浮搜索胶囊）；**`SceneEdit` 场景编辑 sheet**「复制一张再改」（路由 × 隐私档不合法组合行内提示、术语桶、入口绑定；若砍范围则随 `.scene` 一起推到 v1.1，见待决策） | 术语命中 → 译名一致可演示；账本与实际出站字节一致 | 决定隐私默认档 |
| **I8 稳定与灰度** | 蓝牙 / OEM 矩阵回归、30 分钟功耗复测、合规文案（权限用途、录音告知、Play 前台服务声明）、崩溃兜底、TestFlight / 内测 apk | 全链路回归通过；关卡 A / B 指标不回退 | TestFlight 上传、内测名单、收集反馈 |

## 页面落地对照（原型 → 里程碑）

原型见 [14-页面原型与设计系统](14-页面原型与设计系统.md) 与 `docs/原型/`。每一屏只归一个里程碑；页面在该里程碑内以原型为验收依据（布局、控件、交互三项对得上）。

| 原型画板 | 里程碑 | 备注 |
|---|---|---|
| `Home` 场景 Tab、`Live` 实时 Tab、`Settings` 设置 Tab、四 Tab 壳 | **I2.5** | 新增步骤：I0 只做了 material3 占位，之前没有任何一步把占位换成设计系统 |
| `LiveM0` 仅听、`LiveM4` 速译一句、`KeyWallet` Key 钱包 sheet、`Onboarding` 第 2 页 | **I3** | |
| `LiveM1` 耳听·面屏、`LiveM3` 双屏对话、`Onboarding` 第 1 / 3 页 | **I4** | |
| `Meeting` 录音中、`MeetingResult` 纪要、`LiveEnd` 对话卡片、`Library` 资料库列表页 | **I5** | 资料库列表页原先无归属，归 I5：产物落库的同一步做"能看见" |
| `ScreenIntake` 屏内入口、`ScreenS4` 播放器 | **I6** | |
| `Ledger` 去向账本、`Settings` 入口 / 隐私组、`Library` 搜索、`LiveEnd` 新词确认、`SceneEdit` 场景编辑 sheet | **I7** | `SceneEdit` 原先无归属：01 篇要求高级选项只出现在"复制一张再改"里，但 `.scene` 导入导出已推到 v1.1——归 I7，砍范围时整体推 v1.1 |

设计系统本身（`core/designsystem`：Tokens / SceneTheme / Liquid Glass / 全部组件 / 预览页 / 对比度单测）已于 2026-09-17 落地，见 `docs/验收记录/design-system.md`；iOS 的 Compose 自绘背景模糊因 Skiko 崩溃关闭，iOS 26+ 原生功能层宿主（待验证 48）另排在 I8 之前视关卡 B 结果决定。

## 工程结构（I0 落地）

```
Translator/
├─ gradle/libs.versions.toml         版本目录（唯一真相源）
├─ shared/                            KMP 库（commonMain / androidMain / iosMain）
│  └─ src/commonMain/kotlin/dev/scenenote/
│     ├─ core/model      Lang / Segment / Transcript / Scene / PrivacyMode / RoutePolicy / LiveMode …（附录 A 子集）
│     ├─ core/db         SQLDelight 库表：sessions / segments / polish_revisions / glossary / egress_ledger
│     ├─ core/settings   AppSettings（multiplatform-settings）+ SecureStore（expect/actual：Keychain / Keystore）
│     ├─ core/egress     EgressGate（唯一出网口）+ EgressLedger（出站账本）+ HttpClient 工厂
│     ├─ core/designsystem  07 篇 §7.14 的 core/design-system：Palette / Tokens（语义色、Apple 字体刻度、间距 4/8/16/20、圆角 10/14/20/28）、SceneTheme、Liquid Glass（GraphicsLayer 背景模糊 + 无障碍回退）、TabBar / NavBar / Dock / Sheet / InsetGroupedList / Toggle / SegmentedControl / Button 四档 / Capsule / Menu / Alert；不依赖 compose.material3；对比度单测 TokensContrastTest
│     ├─ audio           AudioCapture / AudioRoute / AudioSink（expect/actual）+ PcmFrame
│     ├─ asr             AsrEngine 接口 + EngineSelector（RoutePolicy × PrivacyMode）；系统识别仅预留插件接口位
│     ├─ nmt             OnnxNmtTranslator（ORT seq2seq + SentencePiece + opus-mt 语言对包，默认离线档）+ NmtModelRegistry
│     ├─ live            LiveSessionMachine（Idle / Arming / Live / Paused / Ending）+ FastPath 阶段接口
│     ├─ ui              主题（迁移期双栈：SceneTheme 外层 + MaterialTheme 内层）、首页场景卡、实时页、设置页（Key 钱包）、ui/gallery 设计系统预览页（scenenote://gallery）
│     └─ di              Koin 模块
├─ androidApp/                        Android 壳（MainActivity、前台服务位、Manifest 权限）
├─ iosApp/                            Xcode 工程（SwiftUI 壳，embedAndSignAppleFrameworkForXcode）
├─ bench/                             延迟 / 功耗基准脚本与验收记录模板（I1 起）
└─ .github/workflows/build.yml        双端构建
```

## 工作方式

- **分支与提交**：`main` 保持可构建；每个里程碑一个分支 `i0-baseline`、`i1-audio`…，验收通过后合并。提交信息前缀 `I0:`、`I1:`。
- **验收记录**：`docs/验收记录/I{n}.md` 记录验收项、真机型号、数据、未达标项与处置；关卡 A / B 单独成文。
- **不做的事**：不写商业化；不接任何自建后端；不内置 Key；测试用 Key 只放本机 SecureStore。
- **文档同步**：里程碑带来的设计变更回写到对应章节（02–08、附录 A），而不是只改代码。

## 风险与应对（执行层）

| 风险 | 应对 |
|---|---|
| AGP 9 + KMP 新插件坑多 | I0 严格照官方模板组合；不用 `android.library` 老插件 |
| sherpa-onnx iOS cinterop 链接失败 | I2 先用官方 `build-ios.sh` 产物 + `c-api.h`，失败则退到 Swift 桥（方案已列） |
| `core/nmt` 与 sherpa-onnx 共用 ORT 出现符号 / 版本冲突，或 opus-mt 在基线机延迟 / 发热超标 | I2 先验证单副本链接；不可解时 NMT 经 sherpa 暴露的 ORT 句柄或独立命名空间；基线机超标则离线档改逐句非流式、beam 1，关卡 A 记录数据后再定 |
| 关卡 A 基线机不达标 | 按路线图预案：基线机默认关声纹改固定方向、greedy 解码；不降低旗舰机指标 |
| 单人真机矩阵不够 | 优先 AirPods + 一副国产 TWS + 一台小米 / 华为；其余机型写入待验证，不承诺 |
| 范围回涨 | 任何新功能先问"直接服务实时线或会议卡吗"，否则进 v1.1 清单 |
