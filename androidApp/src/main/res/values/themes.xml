<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 冷启动窗口：底色与 Compose 开屏首帧一致（都是页面底色），避免白闪；系统栏也铺同一支色，不留一条深色的状态栏。
         内容层的配色仍由 SceneTheme 决定 -->
    <style name="Theme.SceneNote" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/brand_splash</item>
        <item name="android:navigationBarColor">@color/brand_splash</item>
        <item name="android:windowLightStatusBar">@bool/brand_light_system_bars</item>
    </style>
</resources>
