<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- 系统字幕（屏内 S1）：抓其他 App 的声音要投屏授权 + 前台服务类型 mediaProjection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- targetSdk ≥ 30 下 TextToSpeech.engines 需要声明可见性，否则国产 ROM 的系统 TTS 引擎可能探测不到 -->
    <queries>
        <intent><action android:name="android.intent.action.TTS_SERVICE" /></intent>
    </queries>

    <application
        android:name="dev.scenenote.android.SceneNoteApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name="dev.scenenote.android.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|locale|layoutDirection"
            android:supportsPictureInPicture="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          深链 / 分享的跳板（无 UI，不进最近任务，独立 task）：VIEW scenenote:// 与 ACTION_SEND 都先到这里，
          转成进程内的 DeepLinks.pending 再拉起 MainActivity（普通 MAIN intent）。
          若让 MainActivity 直接接 VIEW，task 根 intent 就是那条深链：进程被杀后从桌面 / 安装器再打开会被系统原样重放
          （vivo Android 16 实测：`scene/listen?autostart=1` 反复自动开始录音）。
        -->
        <activity
            android:name="dev.scenenote.android.DeepLinkActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="scenenote" />
            </intent-filter>
            <!-- 分享面板进屏内字幕（S4）：其他 App「分享」一个视频 / 音频到本 App，复制后发 scenenote://screen?shared= 深链 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <!-- 系统字幕前台服务（屏内 S1）：mediaProjection|microphone；只做 startForeground → getMediaProjection → 交给共享层 -->
        <service
            android:name="dev.scenenote.android.CaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone" />

        <!-- 主屏小组件（I7 最小入口）：「仅听」「会议」两枚按钮 → 深链 -->
        <receiver android:name="dev.scenenote.android.SceneWidgetReceiver" android:exported="true" android:label="场记">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/scene_widget_info" />
        </receiver>

        <!-- 本地通知（I7）：AlarmManager 到点广播 → 发通知；点开走 VIEW 深链 -->
        <receiver android:name="dev.scenenote.core.platform.NotifyReceiver" android:exported="false" />

        <!-- 快捷设置磁贴（规格 §3.1 Android 触发入口）：点击 → scenenote:// 深链进会话页；只做入口，不做开关 -->
        <service
            android:name="dev.scenenote.android.ListenTileService"
            android:exported="true"
            android:label="仅听"
            android:icon="@drawable/ic_tile_listen"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE" android:value="false" />
        </service>
        <service
            android:name="dev.scenenote.android.QuickPhraseTileService"
            android:exported="true"
            android:label="速译一句"
            android:icon="@drawable/ic_tile_quick_phrase"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE" android:value="false" />
        </service>
        <!-- 「屏内翻译」（屏内 S1）：在 YouTube 等 App 里下拉点一下 → 系统投屏弹窗 → 抓取开始 → 切回视频时自动进画中画字幕条 -->
        <service
            android:name="dev.scenenote.android.ScreenCaptionTileService"
            android:exported="true"
            android:label="@string/tile_screen_caption"
            android:icon="@drawable/ic_tile_screen_caption"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE" android:value="false" />
        </service>

        <!-- 系统分享（I5 会议卡 / 资料库导出）：把 filesDir / cacheDir 下的 .md / .voxnote / .png 以 content:// 交给分享面板 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
